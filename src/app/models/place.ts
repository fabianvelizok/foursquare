export interface Place {
  id: string,
  name: string,
  description: string;
  subscription: string;
  active: boolean;
  street: string;
  city: string;
  country: string;
  lat: number;
  lng: number;
}
